# from tkinter import *
# import threading
#
# class UI(threading.Thread):
#
#     def __init__(self):
#         threading.Thread.__init__(self)
#         self.start()
#
# speechRepresentation = Tk()
# speechRepresentation.title("Speech Recognition")
# lbl = Label(speechRepresentation, text="This is what you have said")
# speechRepresentation.mainloop()


        # while loop:
        #     self.speechRepresentation.update()
        #     lbl = Label(self.speechRepresentation, text=userText)
        #     lbl.pack()
        # self.speechRepresentation.quit()